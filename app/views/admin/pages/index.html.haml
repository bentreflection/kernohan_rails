%h1 Pages
%table.sortable
  %thead
    %tr
      %th Nav Title
      %th URL
      %th Tagline
      %th= link_to "New Page", new_admin_page_path, :class => "header_link add", :title => "Add a Page"
  %tbody
    - if @pages.empty?
      %tr
        %td{:colspan => 5} No Pages have been created yet.
    - for page in @pages do
      %tr{:id => "page_#{page.id}"}
        %td=page.nav_title
        %td=link_to "/pages/#{page.slug}", "/pages/#{page.slug}"
        %td=h(truncate(page.tagline, :length => 50))
        %td.actions
          =link_to 'edit', edit_admin_page_path(page), :class => 'edit', :title => "Edit Page"
          =link_to 'delete', admin_page_path(page), :class => 'delete', :title => "Delete Page", :method => :delete, :confirm => "Are you sure you want to delete this Page? This cannot be undone."
          = link_to "Sort", update_positions_admin_pages_path, :class => "sort", :title => "Sort pages"
